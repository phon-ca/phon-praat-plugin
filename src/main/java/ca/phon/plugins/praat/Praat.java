package ca.phon.plugins.praat;

import ca.phon.session.Record;

/**
 * Praat is the extension for Record objects which provides
 * access to the underlying database.
 */
public class Praat {
	
	private final Record record;
	
	public Praat(Record record) {
		super();
		this.record = record;
	}

	
	
}
